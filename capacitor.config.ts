import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.furepet.app',
  appName: 'FurePet',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;